import { images } from "../constants";

const NavBar = () => {
  return (
    <nav>
      <div>
        <img src={images.Logo} alt="logo" />
      </div>
      <div></div>
    </nav>
  );
};

export default NavBar;
